// Vercel bundles this catch-all file as a Node.js serverless function. The
// Express application is shared with local development, so API behavior stays
// identical in both environments.
export { default } from "../server/index.js";
